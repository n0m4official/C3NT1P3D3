# Add this to your CMakeLists.txt before the "Tests" section:

# -----------------------------
# Production executable (README-compliant)
# -----------------------------
if(EXISTS "${PROJECT_SOURCE_DIR}/src/C3NT1P3D3-Production.cpp")
    # Collect core source files (exclude demo mains)
    set(CORE_SRC_FILES
        "${PROJECT_SOURCE_DIR}/src/core/ConfigurationManager.cpp"
        "${PROJECT_SOURCE_DIR}/src/core/ProductionScanner.cpp"
        "${PROJECT_SOURCE_DIR}/src/simulation/SimulationEngine.cpp"
        "${PROJECT_SOURCE_DIR}/src/IPRangeValidator.cpp"
        "${PROJECT_SOURCE_DIR}/src/VulnerabilityDatabase.cpp"
    )
    
    add_executable(C3NT1P3D3-Comprehensive 
        "${PROJECT_SOURCE_DIR}/src/C3NT1P3D3-Production.cpp"
        ${CORE_SRC_FILES}
    )
    
    if(Threads_FOUND)
        target_link_libraries(C3NT1P3D3-Comprehensive PRIVATE Threads::Threads)
    endif()
    
    message(STATUS "Added C3NT1P3D3-Comprehensive production target.")
endif()
